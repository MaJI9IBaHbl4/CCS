<Page x:Class="CustomCodeSystem.Pages.PageScanOperations"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:CustomCodeSystem.Pages"
      mc:Ignorable="d"
      d:DesignHeight="700" d:DesignWidth="1800"
      Title="PageScanOperations"
      Background="#F6F7FB">

    <Page.Resources>

        <Style x:Key="CardShadowStyle" TargetType="Border">
            <Setter Property="Background" Value="#FFFFFFFF"/>
            <Setter Property="CornerRadius" Value="14"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="IsHitTestVisible" Value="False"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="14" ShadowDepth="2" Opacity="0.14"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CardSurfaceStyle" TargetType="Border">
            <Setter Property="Background" Value="#FFFFFFFF"/>
            <Setter Property="CornerRadius" Value="14"/>
            <Setter Property="Padding" Value="14,12"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="TextOptions.TextFormattingMode" Value="Display"/>
            <Setter Property="TextOptions.TextRenderingMode" Value="ClearType"/>
            <Setter Property="TextOptions.TextHintingMode" Value="Fixed"/>
            <Setter Property="RenderOptions.ClearTypeHint" Value="Enabled"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
        </Style>

        <Style x:Key="SectionTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#000000"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>

        <Style x:Key="LabelText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#000000"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
        </Style>

        <Style x:Key="HintText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#000000"/>
            <Setter Property="Opacity" Value="0.75"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>

        <Style x:Key="CountText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#000000"/>
            <Setter Property="Margin" Value="0,10,0,0"/>
        </Style>

        <Style x:Key="ErrorText" TargetType="TextBlock" BasedOn="{StaticResource HintText}">
            <Setter Property="Foreground" Value="#B91C1C"/>
            <Setter Property="Opacity" Value="1"/>
            <Setter Property="Margin" Value="0,6,0,0"/>
        </Style>

        <Style x:Key="ReadableTextBox" TargetType="TextBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#000000"/>
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,7"/>
            <Setter Property="Height" Value="34"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="TextOptions.TextFormattingMode" Value="Display"/>
            <Setter Property="TextOptions.TextRenderingMode" Value="ClearType"/>
            <Setter Property="TextOptions.TextHintingMode" Value="Fixed"/>
            <Setter Property="RenderOptions.ClearTypeHint" Value="Enabled"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid SnapsToDevicePixels="True" UseLayoutRounding="True">
                            <Border x:Name="shadow"
                                    CornerRadius="10"
                                    Background="#FFFFFF"
                                    BorderBrush="Transparent"
                                    BorderThickness="1"
                                    IsHitTestVisible="False">
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="10"
                                                      ShadowDepth="2"
                                                      Opacity="0.14"/>
                                </Border.Effect>
                            </Border>

                            <Border x:Name="bd"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="10"
                                    SnapsToDevicePixels="True">
                                <ScrollViewer x:Name="PART_ContentHost"
                                              Margin="0"
                                              VerticalAlignment="Center"/>
                            </Border>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="BorderBrush" Value="#9CA3AF"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="bd" Property="BorderBrush" Value="#2563EB"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="bd" Property="Opacity" Value="0.55"/>
                                <Setter TargetName="shadow" Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ReadableTextBoxReadonly" TargetType="TextBox" BasedOn="{StaticResource ReadableTextBox}">
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="Background" Value="#F9FAFB"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
        </Style>

        <Style x:Key="ReadableCheckBox" TargetType="CheckBox">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#000000"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="TextOptions.TextFormattingMode" Value="Display"/>
            <Setter Property="TextOptions.TextRenderingMode" Value="ClearType"/>
            <Setter Property="TextOptions.TextHintingMode" Value="Fixed"/>
            <Setter Property="RenderOptions.ClearTypeHint" Value="Enabled"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
        </Style>

        <Style x:Key="RoundedRadioButton" TargetType="RadioButton">
            <Setter Property="Height" Value="34"/>
            <Setter Property="MinWidth" Value="140"/>
            <Setter Property="Padding" Value="14,7"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#000000"/>
            <Setter Property="Background" Value="#F3F4F6"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#EEF2FF"/>
                                <Setter TargetName="bd" Property="BorderBrush" Value="#9CA3AF"/>
                            </Trigger>

                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#111827"/>
                                <Setter TargetName="bd" Property="BorderBrush" Value="#111827"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>

                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="bd" Property="Opacity" Value="0.55"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ReadableButton" TargetType="Button">
            <Setter Property="Height" Value="34"/>
            <Setter Property="MinWidth" Value="84"/>
            <Setter Property="Padding" Value="14,7"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#000000"/>
            <Setter Property="Background" Value="#F3F4F6"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="TextOptions.TextFormattingMode" Value="Display"/>
            <Setter Property="TextOptions.TextRenderingMode" Value="ClearType"/>
            <Setter Property="TextOptions.TextHintingMode" Value="Fixed"/>
            <Setter Property="RenderOptions.ClearTypeHint" Value="Enabled"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid SnapsToDevicePixels="True" UseLayoutRounding="True">
                            <Border x:Name="shadow"
                                    CornerRadius="10"
                                    Background="#FFFFFF"
                                    BorderBrush="Transparent"
                                    BorderThickness="1"
                                    IsHitTestVisible="False">
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="10"
                                                      ShadowDepth="2"
                                                      Opacity="0.14"/>
                                </Border.Effect>
                            </Border>

                            <Border x:Name="bd"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="10"
                                    SnapsToDevicePixels="True">
                                <ContentPresenter HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"
                                                  RecognizesAccessKey="True"/>
                            </Border>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#EEF2FF"/>
                                <Setter TargetName="bd" Property="BorderBrush" Value="#9CA3AF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#E5E7EB"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="bd" Property="Opacity" Value="0.55"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource ReadableButton}">
            <Setter Property="Background" Value="#111827"/>
            <Setter Property="BorderBrush" Value="#111827"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Opacity" Value="0.92"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bd" Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="bd" Property="Opacity" Value="0.55"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ReadableDataGrid" TargetType="DataGrid">
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="CanUserResizeRows" Value="False"/>
            <Setter Property="CanUserSortColumns" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="SelectionUnit" Value="FullRow"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="RowHeaderWidth" Value="0"/>
            <Setter Property="RowBackground" Value="#FFFFFF"/>
            <Setter Property="AlternatingRowBackground" Value="#F9FAFB"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Foreground" Value="#000000"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
        <Style x:Key="FindProductsStatusTextStyle" TargetType="TextBlock">
            <!-- по умолчанию: ошибка -->
            <Setter Property="Foreground" Value="#B91C1C"/>
            <Setter Property="FontWeight" Value="SemiBold"/>

            <Style.Triggers>
                <DataTrigger Binding="{Binding FindProductsStatus}" Value="OK">
                    <Setter Property="Foreground" Value="#166534"/>
                </DataTrigger>

                <DataTrigger Binding="{Binding FindProductsStatus}" Value="Loading...">
                    <Setter Property="Foreground" Value="#1D4ED8"/>
                </DataTrigger>

                <DataTrigger Binding="{Binding FindProductsStatus}" Value="Waiting...">
                    <Setter Property="Foreground" Value="#92400E"/>
                </DataTrigger>

                <DataTrigger Binding="{Binding FindProductsStatus}" Value="Skipped">
                    <Setter Property="Foreground" Value="#6B7280"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CreateActionStatusTextStyle" TargetType="TextBlock">
            <!-- по умолчанию: ошибка -->
            <Setter Property="Foreground" Value="#B91C1C"/>
            <Setter Property="FontWeight" Value="SemiBold"/>

            <Style.Triggers>
                <DataTrigger Binding="{Binding CreateActionStatus}" Value="OK">
                    <Setter Property="Foreground" Value="#166534"/>
                </DataTrigger>

                <DataTrigger Binding="{Binding CreateActionStatus}" Value="Loading...">
                    <Setter Property="Foreground" Value="#1D4ED8"/>
                </DataTrigger>

                <DataTrigger Binding="{Binding CreateActionStatus}" Value="Waiting...">
                    <Setter Property="Foreground" Value="#92400E"/>
                </DataTrigger>

                <DataTrigger Binding="{Binding CreateActionStatus}" Value="Skipped">
                    <Setter Property="Foreground" Value="#6B7280"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Page.Resources>

    <Grid Margin="18"
          UseLayoutRounding="True"
          SnapsToDevicePixels="True"
          TextOptions.TextFormattingMode="Display"
          TextOptions.TextRenderingMode="ClearType"
          TextOptions.TextHintingMode="Fixed"
          RenderOptions.ClearTypeHint="Enabled">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid x:Name="gridError"
              Grid.Row="0"
              Visibility="Visible"
              VerticalAlignment="Center"
              HorizontalAlignment="Center">
            <StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Klaida bandant paleisti GOR operacija: "/>
                    <TextBlock x:Name="textBlockErrorGorCode"/>
                </StackPanel>

                <TextBlock x:Name="gridErrorErrorText"/>

                <Button x:Name="gridErrorBtn"
                        Content="Bandyti dar karta"
                        Click="gridErrorBtn_Click"
                        Style="{StaticResource PrimaryButton}"/>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1" x:Name="gridMain" Margin="100,0,100,50">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

                <StackPanel Margin="0,0,50,0">

                    <Grid 
                          SnapsToDevicePixels="True"
                          UseLayoutRounding="True"
                          Visibility="Collapsed">
                        <Border Style="{StaticResource CardShadowStyle}"/>
                        <Border Style="{StaticResource CardSurfaceStyle}" Margin="0">
                            <StackPanel x:Name="stackGorCode">
                                <TextBlock Style="{StaticResource SectionTitle}" Text="GOR kodas"/>
                                <TextBlock Style="{StaticResource LabelText}" Text="Įvesk GOR kodą"/>

                                <TextBox x:Name="textBoxGorCode"
                                         Style="{StaticResource ReadableTextBox}"
                                         KeyDown="textBoxGorCode_KeyDown"/>

                                <TextBlock x:Name="textBlockGorCodeError"
                                           Style="{StaticResource ErrorText}"/>
                            </StackPanel>
                        </Border>
                    </Grid>

                    <Grid Margin="0,0,0,12" SnapsToDevicePixels="True" UseLayoutRounding="True">
                        <Border Style="{StaticResource CardShadowStyle}"/>
                        <Border Style="{StaticResource CardSurfaceStyle}" Margin="0">
                            <StackPanel>
                                <TextBlock Style="{StaticResource SectionTitle}" Text="Operacijos informacija"/>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,12,10">
                                        <TextBlock Style="{StaticResource LabelText}" Text="Operacijos kodas"/>
                                        <TextBox x:Name="textBoxOperationCode"
                                                 Style="{StaticResource ReadableTextBoxReadonly}"/>
                                    </StackPanel>

                                    <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,12,10">
                                        <TextBlock Style="{StaticResource LabelText}" Text="Operacijos aprašymas"/>
                                        <TextBox x:Name="textBoxOperationDiscription"
                                                 Style="{StaticResource ReadableTextBoxReadonly}"/>
                                    </StackPanel>

                                    <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,0,0,10">
                                        <TextBlock Style="{StaticResource LabelText}" Text="Privalomos operacijos"/>
                                        <TextBox x:Name="textBoxMandatoryOperations"
                                                 Style="{StaticResource ReadableTextBoxReadonly}"/>
                                    </StackPanel>

                                    <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,0,0,10">
                                        <TextBlock Style="{StaticResource LabelText}" Text="Kartojama"/>
                                        <CheckBox x:Name="checkBoxRepeatable"
                                                  Style="{StaticResource ReadableCheckBox}"
                                                  Content="Repeatable"
                                                  IsEnabled="False"/>
                                    </StackPanel>
                                </Grid>

                                <StackPanel x:Name="stackMain" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>
                    </Grid>

                    <Grid x:Name="boarderScan" Margin="0,0,0,12" SnapsToDevicePixels="True" UseLayoutRounding="True">
                        <Border Style="{StaticResource CardShadowStyle}"/>
                        <Border Style="{StaticResource CardSurfaceStyle}" Margin="0">
                            <StackPanel>
                                <TextBlock Style="{StaticResource SectionTitle}" Text="Skenavimas"/>
                                <TextBlock Style="{StaticResource LabelText}" Text="Skenuok gaminio custom serijinį numerį"/>

                                <TextBox x:Name="textBoxInput"
                                         Style="{StaticResource ReadableTextBox}"
                                         KeyDown="textBoxInput_KeyDown"/>

                                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                    <RadioButton x:Name="radioGood"
                                                 Content="Geras"
                                                 GroupName="ScanResult"
                                                 Style="{StaticResource RoundedRadioButton}"/>

                                    <RadioButton x:Name="radioBad"
                                                 Content="Blogas"
                                                 GroupName="ScanResult"
                                                 Style="{StaticResource RoundedRadioButton}"
                                                 Margin="10,0,0,0"/>
                                </StackPanel>

                                <TextBlock x:Name="textBlockCompletedCount"
                                           Style="{StaticResource CountText}"
                                           Text="Atlikta operaciju: 0"/>

                                <TextBlock x:Name="textBlockScanError"
                                           Style="{StaticResource ErrorText}"/>
                            </StackPanel>
                        </Border>
                    </Grid>

                
            </StackPanel>
            <Grid Grid.Column="1" Margin="0,0,0,12" SnapsToDevicePixels="True" UseLayoutRounding="True">
                <Border Style="{StaticResource CardShadowStyle}"/>
                <Border Style="{StaticResource CardSurfaceStyle}" Margin="0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0"
                       Style="{StaticResource SectionTitle}"
                       Text="Skenavimo rezultatai"/>

                        <DataGrid Grid.Row="1"
                      x:Name="scanResultsGrid"
                      Style="{StaticResource ReadableDataGrid}"
                      Margin="0,6,0,0">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Skenuoti duomenys"
                                        Binding="{Binding ScannedData}"
                                        Width="2*"/>

                                <DataGridTextColumn Header="FindProductsAsync"
                    Binding="{Binding FindProductsStatus}"
                    Width="2*"
                    ElementStyle="{StaticResource FindProductsStatusTextStyle}"/>

                                <DataGridTextColumn Header="CreateActionByOperationalCodeAsync"
                    Binding="{Binding CreateActionStatus}"
                    Width="2*"
                    ElementStyle="{StaticResource CreateActionStatusTextStyle}"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</Page>