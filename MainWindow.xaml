<Window x:Class="CustomCodeSystem.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CustomCodeSystem"
        mc:Ignorable="d"
        Title="Custom Code System"
        Height="900"
        Width="1600"
        WindowState="Maximized"
        WindowStartupLocation="CenterScreen"
        Background="#F6F7FB">

    <Window.Resources>

        <!-- ====== CARD: shadow + surface (crisp text) ====== -->
        <Style x:Key="CardShadowStyle" TargetType="Border">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="CornerRadius" Value="14"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="IsHitTestVisible" Value="False"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="18" ShadowDepth="3" Opacity="0.12"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CardSurfaceStyle" TargetType="Border">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="CornerRadius" Value="14"/>
            <Setter Property="Padding" Value="14,10"/>
            <Setter Property="BorderBrush" Value="#E6E8EF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
        </Style>

        <Style x:Key="CardSurfaceMainStyle" TargetType="Border" BasedOn="{StaticResource CardSurfaceStyle}">
            <Setter Property="Background" Value="#F6F7FB"/>
        </Style>

        <!-- Global crisp text -->
        <Style TargetType="TextBlock">
            <Setter Property="TextOptions.TextFormattingMode" Value="Display"/>
            <Setter Property="TextOptions.TextRenderingMode" Value="ClearType"/>
            <Setter Property="TextOptions.TextHintingMode" Value="Fixed"/>
            <Setter Property="RenderOptions.ClearTypeHint" Value="Enabled"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
        </Style>

        <!-- Header text styles -->
        <Style x:Key="HeaderTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#000000"/>
        </Style>

        <Style x:Key="MetaLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Foreground" Value="#000000"/>
            <Setter Property="Opacity" Value="0.55"/>
        </Style>

        <Style x:Key="MetaValue" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#000000"/>
            <Setter Property="Opacity" Value="0.85"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
        </Style>

        <!-- Version badge -->
        <Style x:Key="VersionBadge" TargetType="Border">
            <Setter Property="Background" Value="#111827"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
        </Style>

        <Style x:Key="VersionBadgeText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <!-- Rounded button (same vibe as your logout) -->
        <Style x:Key="RoundedButton" TargetType="Button">
            <Setter Property="Height" Value="35"/>
            <Setter Property="MinWidth" Value="100"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#000000"/>
            <Setter Property="Background" Value="#F3F4F6"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#EEF2FF"/>
                                <Setter TargetName="bd" Property="BorderBrush" Value="#CBD5E1"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#E5E7EB"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="bd" Property="Opacity" Value="0.55"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <Grid UseLayoutRounding="True"
          SnapsToDevicePixels="True"
          TextOptions.TextFormattingMode="Display"
          TextOptions.TextRenderingMode="ClearType"
          TextOptions.TextHintingMode="Fixed"
          RenderOptions.ClearTypeHint="Enabled">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- =========================
             HEADER CARD
             ========================= -->
        <Grid Grid.Row="0" Margin="5" SnapsToDevicePixels="True" UseLayoutRounding="True">
            <Border Style="{StaticResource CardShadowStyle}"/>
            <Border Style="{StaticResource CardSurfaceStyle}">

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- LEFT: title + user -->
                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                        <TextBlock Style="{StaticResource HeaderTitle}" Text="Custom Code System"/>

                        <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                            <TextBlock Text="Vartotojas:"
                                       FontSize="12"
                                       Foreground="#000000"
                                       Opacity="0.65"/>

                            <TextBlock x:Name="textBlockUser"
                                       Margin="6,0,0,0"
                                       FontSize="12"
                                       FontWeight="SemiBold"
                                       Foreground="#000000"
                                       Opacity="0.85"
                                       TextTrimming="CharacterEllipsis"
                                       MaxWidth="520"/>
                        </StackPanel>
                    </StackPanel>

                    <!-- CENTER: location (and optionally env) -->
                    <Grid Grid.Column="1"
                          VerticalAlignment="Center"
                          HorizontalAlignment="Right"
                          MaxWidth="650">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0"
                                   Style="{StaticResource MetaLabel}"
                                   Text="Location"
                                   HorizontalAlignment="Center"/>

                        <TextBlock Grid.Row="1"
                                   x:Name="textBlockLocation"
                                   Style="{StaticResource MetaValue}"
                                   Text="—"
                                   HorizontalAlignment="Center"
                                   MaxWidth="650"/>
                    </Grid>

                    <!-- RIGHT: version badge -->
                    <Border Grid.Column="2"
                            Style="{StaticResource VersionBadge}"
                            VerticalAlignment="Center"
                            Margin="50,0,50,0">
                        <TextBlock x:Name="textBlockVersion"
                                   Style="{StaticResource VersionBadgeText}"
                                   Text="v1.0"/>
                    </Border>

                    <!-- RIGHT: logout -->
                    <Button Grid.Column="3"
                            x:Name="btnLogOut"
                            Content="LogOut"
                            Style="{StaticResource RoundedButton}"
                            VerticalAlignment="Center"
                            Click="Logout_Click"/>
                </Grid>

            </Border>
        </Grid>

        <!-- =========================
             MAIN CONTENT CARD
             ========================= -->
        <Grid Grid.Row="1" Margin="5" SnapsToDevicePixels="True" UseLayoutRounding="True">
            <Border Style="{StaticResource CardShadowStyle}"/>
            <Border Style="{StaticResource CardSurfaceMainStyle}">

                <Frame x:Name="MainFrame"
                       NavigationUIVisibility="Hidden"/>
            </Border>
        </Grid>

    </Grid>
</Window>
